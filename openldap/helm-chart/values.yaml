replicaCount: 1

fullnameOverride: "openldap"

image:
  repository: osixia/openldap
  tag: "1.5.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 389

persistence:
  enabled: true
  size: 1Gi
  accessMode: ReadWriteOnce

config:
  LDAP_ORGANISATION: "PTIT"
  LDAP_DOMAIN: "ptit.edu.vn"
  LDAP_BASE_DN: "dc=ptit,dc=edu,dc=vn"

secret:
  LDAP_ADMIN_PASSWORD: "ai-coach@2026"

# Seed data - users for testing
seedUsers:
  - uid: "B23DCCN465"
    cn: "Kien"
    sn: "Nguyen Tu"
    mail: "KienNT.B23CN465@stu.ptit.edu.vn"
    password: "Kien#812005"
    